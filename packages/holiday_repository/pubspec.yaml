name: holiday_repository
description: Holiday repository domain
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  holiday_api:
    path: ../holiday_api
  
dev_dependencies:
  very_good_analysis: ^5.1.0
